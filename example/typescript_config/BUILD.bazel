load("//vitest:defs.bzl", "vitest_test")

vitest_test(
    name = "test",
    node_modules = "//:node_modules",
    config = "vitest.config.ts",
    data = [
        "greeter.js",
        "greeter.test.js",
        "vitest.config.ts",
    ],
)
